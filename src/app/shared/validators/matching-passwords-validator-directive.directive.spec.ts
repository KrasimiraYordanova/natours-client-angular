import { MatchingPasswordsValidatorDirectiveDirective } from './matching-passwords-validator-directive.directive';

describe('MachingPasswordsValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchingPasswordsValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
